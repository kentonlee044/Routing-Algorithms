#!/bin/bash

# Put the command to run your program here.
# For example, if you wish to use Python:
python3 main.py "$@"

# Remember that `"$@"` passes the command-line arguments passed
# to `Routing.sh` directly to your script.